import { useEffect, useRef } from "react"; import { Terminal } from "xterm"; import "xterm/css/xterm.css"; export default function TerminalSexy() { const termRef = useRef(null); useEffect(() => { const term = new Terminal({ theme: { background: "#0f0f0f", foreground: "#00ffcc" }, fontFamily: "Iosevka, monospace", fontSize: 14 }); term.open(termRef.current); term.write("Welcome to terminal.sexy\\r\\n> "); }, []); return <div ref={termRef} className="h-full w-full overflow-hidden rounded-lg border border-cyan-500" />; }
